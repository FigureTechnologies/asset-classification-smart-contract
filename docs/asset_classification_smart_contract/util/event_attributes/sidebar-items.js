window.SIDEBAR_ITEMS = {"enum":[["EventType","An enum that contains all different event types that can occur throughout the contract’s routes."]],"struct":[["EventAdditionalMetadata","A helper collection that allows underlying processes to specify dynamic key values for processes that don’t necessarily need to specify a large amount of new event keys.  Emitted values are aggregated and sorted deterministically, and then displayed using the format: [a=1, b=2, c=3, etc]"],["EventAttributes","A helper struct to emit attributes for a Response."]]};